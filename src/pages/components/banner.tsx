import React from 'react';

export default function Banner(props: { darkMode?: boolean }) {
    return (
        <div>
            <img
                src="img/banner.png"
                alt="Cherry bit"
            />
        </div>
    );
}